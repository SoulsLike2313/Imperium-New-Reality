# Imperium Core Reference Corridor Taskpack

This directory is the extracted, machine-readable Servitor work order for `IMPERIUM-CORE-REFERENCE-CORRIDOR-0001`.

Authority begins as `OWNER_EQUIVALENT_INSTRUCTION` because the current Astronomicon registry points to an unrelated H-TUI task. The pack must pass the repository's digest-bound `servitor_intake.py` gate before implementation begins. Formalization does not imply Owner acceptance, Throne completion, or permission to land.
