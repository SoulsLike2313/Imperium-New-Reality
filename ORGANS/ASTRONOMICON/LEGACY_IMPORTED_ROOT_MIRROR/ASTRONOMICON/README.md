# ASTRONOMICON (NewGen Body)

Astronomicon is the task-essence and route-definition organ.

This body provides:
- task essence contract and schema;
- route packet contract and schema;
- organ route map and route decision registry;
- fixtures and checkers;
- read-only TUI inspector;
- Ghost_Evolve accepted/rejected upgrade indexes.

Scope boundary:
- script-first route intelligence only;
- no IDE implementation;
- no WARP runtime implementation;
- no CLI worker implementation;
- no LLM-agent persona/memory archive.
