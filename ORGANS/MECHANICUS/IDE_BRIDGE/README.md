# Mechanicus IDE Bridge

Task: `TASK-NEWREALITY-MECHANICUS-IMPERIAL-IDE-CONTROL-SHELL-TUI-PC-V0_1`

This bridge exposes read-only registry snapshots and dry-run safety state to Imperial IDE consumers.

Real execution and arbitrary shell access remain blocked. Every tool invocation request must pass through command policy and produce a receipt.
