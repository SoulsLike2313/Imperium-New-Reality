{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Explicit command bridge only; no unbounded shell.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
