# Imperium App Tauri Shell Foundation V0.1

Run later:

```powershell
cd SUPPORT/APP_TAURI
npm install
npm run tauri:dev
```

Build later:

```powershell
cd SUPPORT/APP_TAURI
npm run tauri:build
```

This patch creates scaffold and contracts only. Build/run proof is next patch.
