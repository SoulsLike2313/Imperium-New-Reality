# Runtime Isolation Contract

Default runtime root:

`E:\IMPERIUM_CONTEXT\LOCAL\OFFICIO_AGENTIS\RUNS`

Rules:

- runtime outputs go outside tracked source directories;
- role/settings/pack outputs must be run-scoped;
- receipts stored under run `receipts/`;
- role packs and matrix artifacts are reproducible from source registries.

