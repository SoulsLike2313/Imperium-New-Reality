# Imperium New Reality Full Picture

Task: `TASK-NEWREALITY-IMPERIUM-SELF-ANALYSIS-LIVE-GIT-PICTURE-PC-V0_3`  
Contour: local PC only  
Snapshot time: `2026-06-07T09:32:16Z`  
Mode: read-only self-analysis with report outputs

## Executive finding

New Reality has a clean tracked top-level shape and a large evidence/history body. The active root is now centered on `ORGANS`, `REPORTS`, `SUPPORT`, `AGENTS.md`, and `.gitignore`. A sixth physical top-level directory, ignored `IMPERIUM_NEW_GENERATION`, contains five stale Astronomicon files and is runtime residue rather than active truth.

Local PC Astronomicon admission works. The current task is registered, selected as the expected task, resolved, and acknowledged `PASS_WITH_WARNINGS`. The main operational defect is path discovery drift: active files moved to current-root `ORGANS/...` and `SUPPORT/...`, while several scripts, tool cards, and route read orders still use the removed `IMPERIUM_NEW_GENERATION/...` or root `MATRIX_SPINE/...` layouts.

Verdict: `PASS_WITH_WARNINGS_READY_FOR_CORE_CLEANUP_PLAN`.

## Live shape

| Top-level entry | Physical files | Classification |
|---|---:|---|
| `ORGANS` | 3,922 | Active organ homes plus organ reports and legacy mirrors |
| `REPORTS` | 1,917 | Central evidence history |
| `SUPPORT` | 5,796 | Common support, quarantine, and learning archive |
| `AGENTS.md` | 1 | Active root operating contract |
| `.gitignore` | 1 | Active repository hygiene policy |
| `IMPERIUM_NEW_GENERATION` | 5 | Ignored generated/legacy-prefix residue |

There are `11,642` physical files and `8,218` tracked files. No symlink or junction was found. The difference is explained by ignored caches/residue, task registration files at snapshot time, and ignored content nested in quarantine.

## Zone split

The exclusive physical classification used by this report is:

| Zone | Files | Operational meaning |
|---|---:|---|
| Active core | 1,434 | Current root contracts, tools, registries, task corridors, and required organ bodies |
| Reports/evidence | 2,483 | `REPORTS/**` and active organ report trees; evidence, not authority by location |
| Common support | 637 | Admitted shared support under `SUPPORT/COMMON_IMPERIUM_SUPPORT` |
| Root quarantine | 3,390 | Banned from active use pending explicit salvage/admission |
| Quarantine governance | 3 | Index, policy, and zone README |
| Learning archive | 1,766 | Thirteen indexed legacy/prototype areas; active runtime use banned |
| Organ legacy mirrors | 1,905 | Historical organ mirrors; readable but excluded from active core |
| Generated/runtime | 24 | Ignored old-prefix files, caches, bytecode, and temporary material outside the prior categories |
| Unknown | 0 | Every physical file matched an explicit path rule; semantic status can still be candidate |

## Active core

The candidate core contract defines nine required organs: `ADMINISTRATUM`, `ASTRONOMICON`, `CUSTODES`, `DOCTRINARIUM`, `INQUISITION`, `MECHANICUS`, `OFFICIO_AGENTIS`, `SCHOLA_IMPERIALIS`, and `STRATEGIUM`. All nine life-zone directories exist and the latest organ-life validation report records `PASS`.

`ORGANS/_CORE_GOVERNANCE` and `ORGANS/_POST_WORK_RING` are active auxiliary governance areas. `ORGANS/SPECULUM` exists as a one-file review auxiliary but is not listed in the required-nine contract. It needs an explicit classification, not silent promotion into the core.

The core is structurally readable, but much of its authority remains `CANDIDATE_V0_1`. Structural presence is proven; canon admission is not.

## Support, quarantine, and learning

`SUPPORT/COMMON_IMPERIUM_SUPPORT` is the current shared-support zone. Its `ROOT_IMPORTED_COMMON_SUPPORT` subtree contains useful contracts, Matrix Spine assets, tools, and historical root documents. These are safe to read and may be usable where current support policy names ownership, but imported location alone does not make them canon.

`SUPPORT/QUESTIONABLE_OR_QUARANTINE` contains `5,159` files in total: `3,390` root-quarantine files, `1,766` learning-archive files, and three governance files. Active use is forbidden. The learning archive indexes thirteen old surfaces, including Agent IDE, Sanctum variants, old organ agents, research, runs, artifacts, and visual prototypes.

## Report and evidence history

There are `73` top-level report directories and `2,483` report-zone files when organ report trees are included. This is valuable evidence history, but it creates search noise and repeated generated receipts. Reports must be cited as historical evidence with task/date/verdict context; they must not override current-root contracts or live git truth.

## Generated and runtime residue

The most important residue is ignored `IMPERIUM_NEW_GENERATION/ORGANS/ASTRONOMICON/...`, which contains five files. The active registration skill currently computes its default route config under that old prefix. Because the ignored residue exists, the bad default can appear to work. Removing residue without repairing discovery would expose the route failure.

Other residue consists of `__pycache__`, `.pyc`, and temporary taskpack directories. This task performed no cleanup.

## Dirt and duplication risks

The worktree dirt at analysis start is coherent task-registration dirt: two modified Astronomicon registry files and eleven untracked files in the registered task directory. `HEAD` equals local `origin/master`; no unrelated tracked modification was observed.

Exact hashing found `327` duplicate groups containing `1,500` files and approximately `274,977,578` redundant bytes. The largest cost class is repeated run evidence in root quarantine; another class is repeated templates and placeholder READMEs in taskpacks and the Mechanicus legacy arsenal. These are cleanup candidates, not deletion authorization.

Versioning risks include:

- multiple `V0_1`/`V0_2`/`V0_3` receipt names without a universal current pointer;
- case variants such as `FINAL_OWNER_SUMMARY_RU.md` and `final_owner_summary_ru.md`;
- parallel JSON/Markdown forms whose authority is not always stated;
- active files and legacy report copies of the same scripts;
- tool cards whose recorded entrypoints still use pre-migration paths.

## Astronomicon findings

Local PC admission is proven by the current admission receipt, resolver receipt, route manifest, task ACK, registry entry, and current expected task. All eight routed participation organs are reachable.

The route drift is not a blocker for this PC task because `execute_registration()` returns directly to local PC registration before loading remote route config. It is a repair priority because remote configuration defaults to the old prefix and route read order asks for missing root `MATRIX_SPINE/INDEX/MATRIX_SPINE_INDEX.md`; the actual file is under common support.

VM2 was previously attempted because a dedicated VM2 readiness task explicitly targeted `VM2_UBUNTU_OVER_SSH_FROM_PC`, and the registration skill exposes VM2 as a first-class contour. That historical proof does not make VM2 a dependency of PC tasks. Future PC taskpacks should route locally by manifest target and must not probe or require VM state unless the task explicitly targets a VM contour.

## Mechanicus findings

Mechanicus is partially script-first. Two current scripts live directly under `ORGANS/MECHANICUS/TOOLS`, while registered tool cards point to additional executable tools in `_CORE_GOVERNANCE`, `STRATEGIUM`, and imported Matrix Spine support. Prior receipts prove core-shape validation and physical-migration tooling. The current body is fragmented, and some cards retain stale paths.

Mechanicus can now support local read-only checks, receipt inventory preflight, external-finalization semantics checks, core-shape tool registration, and selected standard-library validators. It is not yet a single current-root tool-control organ with a canonical executable registry, uniform replay state, dependency inventory, or automatic path validation.

## Canon boundary

Safe to read but unsafe to treat as canon without explicit admission:

- all report trees;
- all organ legacy mirrors;
- quarantine and learning archive;
- imported common support without an owner/allowed-use statement;
- Mechanicus arsenal candidate cards;
- candidate core and participation contracts beyond their stated stage;
- ignored runtime residue.

No Emperor Passport or Constitution artifact was found as an active file or by content search. This report therefore proposes a document patch plan but does not invent or write those authorities.

## Recommended next step

Approve `TASK-NEWREALITY-ASTRONOMICON-PC-FIRST-ROOT-DISCOVERY-AND-ROUTE-CONFIG-HARDENING-PC-V0_1`. It should repair root-relative config and Matrix Spine discovery, add legacy fallback warnings, prove that PC registration never depends on VM routes, and validate all recorded entrypoints. Only after that should a separate owner-approved cleanup plan decide what duplicate report/quarantine material can be compacted or retained.

## Evidence

- `LIVE_GIT_ADDENDUM_PC.md`
- `organ_core_map.json`
- `zone_inventory.json`
- `astronomicon_local_pc_registration_and_route_drift_report.md`
- `mechanicus_current_readiness_snapshot.md`
- `command_receipt.json`

