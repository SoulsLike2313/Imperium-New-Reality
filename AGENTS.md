# AGENTS.md - New Reality Root Contract

You are operating inside IMPERIUM_NEW_GENERATION_NEW_REALITY.

Default rules:

1. Read/write/mutate only inside this root.
2. Do not access Ancient Empire or parent folders unless the task explicitly grants admission.
3. If outside context is needed, stop and create a SALVAGE_REQUEST.
4. Follow Officio final response contract.
5. For any non-BLOCK completed task, commit/push or explain the blocker with evidence.
6. No fake green. Claims require receipts.

Current root policy:

1. Active PC truth is this repository root, not `IMPERIUM_NEW_GENERATION`, VM2, VM3, Ancient, report archives, or quarantine by default.
2. When present, read `ORGANS/_CORE_GOVERNANCE/GOVERNANCE_INDEX.json`, Emperor Passport, and Constitution before authority-bearing work.
3. Astronomicon task registry is the task-entry source; PC tasks do not require remote contours unless the task manifest says so.
4. Mechanicus tools require a tool card, registry seed, or explicit candidate status before being treated as reusable capability.
5. Every PASS claim requires receipts, validation evidence, and current git truth.
6. Validated in-scope task outputs may be committed and pushed; push is forbidden for out-of-scope, dirty, secret, failed-validation, or destructive changes.
