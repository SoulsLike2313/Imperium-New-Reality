# Mechanicus Forge Client V0.9 — Steampunk Terminal Contract

## Mode

READ_ONLY_STEAMPUNK_TUI.

## Hard truth

This patch improves terminal-native aesthetics only.
It cannot create real bitmap heraldry or metal textures.

## Preserved

- read-only mode;
- no install actions;
- no registry writes;
- no evidence writes;
- no server;
- no hidden background process;
- action sockets remain sealed.

## Future path

If Owner wants the generated reference almost literally:
- keep this TUI as operational core;
- add enhanced visual layer later through webview/canvas or image-capable terminal mode;
- bind that layer to the same backend truth.
