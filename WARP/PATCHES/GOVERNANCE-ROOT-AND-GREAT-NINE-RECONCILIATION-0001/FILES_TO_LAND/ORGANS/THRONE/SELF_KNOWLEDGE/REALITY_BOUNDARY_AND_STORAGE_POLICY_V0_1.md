# REALITY BOUNDARY AND STORAGE POLICY V0.1

patch_id: `GOVERNANCE-ROOT-AND-GREAT-NINE-RECONCILIATION-0001`  
owner: `THRONE`  
status: `ACTIVE_ROOT_LAW`

## One-line law

`E:\IMPERIUM_REALITY` is Core Reality, not a dump and not the final home of WARP or HARNESS.

## Pillars

### REALITY / CORE

Canonical home:

```text
E:\IMPERIUM_REALITY
```

Meaning:

```text
what Imperium is
active organs
Throne
validators
matrices
schemas that define core law
receipts/reports that are current canon proof
permanent core tools that are required constantly by the core
```

### WARP

Canonical home:

```text
E:\IMPERIUM_WARP
```

Meaning:

```text
where Imperium changes
patch packs
task packs
temporary work
land/abort boundary
```

Current `WARP/` inside Reality is transitional debt until externalization.

### HARNESS

Canonical home:

```text
E:\IMPERIUM_HARNESS
```

Meaning:

```text
where Imperium proves and trains
evidence memory
test environments
external tools
IDE foundation
benchmark outputs
independent product workspaces
```

Current `_HARNESS/` inside Reality is transitional debt until externalization.

### SUPPORT

Canonical home:

```text
E:\IMPERIUM_REALITY\SUPPORT
```

Meaning:

```text
transport
quarantine
migration
compatibility support
```

Support is service infrastructure, not a fourth power pillar.

## Root law

Allowed root entries:

```text
ORGANS/
SUPPORT/
AGENTS.md
README.md
.gitignore
.gitattributes
.editorconfig
```

Transitional debt root entries:

```text
WARP/
_HARNESS/
```

Forbidden active root drift:

```text
DOCTRINARIUM/
SCHEMAS/
REPORTS/
APPLY_*.ps1
*_FILE_MANIFEST_SHA256.json
```

## Drift handling

Useful files are not deleted silently.

They are moved with SHA256 proof to:

```text
ORGANS/DOCTRINARIUM/LEGACY_ROOT_IMPORT/
ORGANS/DOCTRINARIUM/SCHEMAS/
SUPPORT/QUARANTINE/
```

## LLM readability target

An external LLM must be able to understand within 30 seconds:

```text
where canon lives
where work is born
where evidence/tools live
where not to write
which zones are transitional debt
which zones are quarantine
```
