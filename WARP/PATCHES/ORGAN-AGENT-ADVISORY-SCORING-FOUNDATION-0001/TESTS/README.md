# TESTS — ORGAN-AGENT-ADVISORY-SCORING-FOUNDATION-0001

Expected:

- all 10 organs have advisory profiles;
- scoring weights sum to 1.0;
- advisory tool generates 10 ranked attention zones;
- advisory language has no direct concrete action commands;
- launcher commands `advise` and `advise organ ASTRONOMICON` run;
- validator returns `PASS_ORGAN_AGENT_ADVISORY_SCORING_READY`.
