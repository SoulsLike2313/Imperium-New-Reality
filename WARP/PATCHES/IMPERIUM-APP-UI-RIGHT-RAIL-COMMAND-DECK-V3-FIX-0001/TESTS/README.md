# TESTS — IMPERIUM-APP-UI-RIGHT-RAIL-COMMAND-DECK-V3-FIX-0001

Validator checks:

- `styles.css` exists;
- v3 ornament/material extension is applied;
- `styles.css` exceeds the previous 22000-character gate;
- previous v3 validator reruns and passes;
- previous v3 receipt becomes PASS.
