# TESTS — IMPERIUM-TUI-ASTRONOMICON-CUSTODES-THRONE-CONSOLE-0001

Expected:

- TUI python script exists;
- TUI PowerShell wrapper exists;
- actions manifest parses;
- all actions have Russian labels/descriptions;
- all actions require aquarium logs;
- `--list-actions` runs;
- `--action status` runs and prints log path;
- `--action throne-readout` runs and shows anti-self-deception field;
- validator returns `PASS_IMPERIUM_TUI_ASTRONOMICON_CONSOLE_READY`.
