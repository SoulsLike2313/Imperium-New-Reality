# TESTS — IMPERIUM-LAUNCHER-APP-PLATFORM-0001

Expected:

- app manifest parses;
- theme parses;
- app PowerShell files exist;
- app has tabs, action list, aquarium log, run/copy/clear/open logs controls;
- app selftest passes;
- app reports action_count >= 8;
- app does not execute or offer git land;
- validator returns `PASS_IMPERIUM_LAUNCHER_APP_PLATFORM_READY`.
