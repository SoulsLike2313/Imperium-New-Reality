# TESTS — IMPERIUM-TUI-WINDOWED-AQUARIUM-SELFTEST-JSON-HOTFIX-0001

Expected:

- fixed validator parses;
- validator extracts selftest JSON from noisy PowerShell stdout;
- selftest PASS + manifest action_count >= 8 is accepted;
- validator returns `PASS_IMPERIUM_TUI_WINDOWED_AQUARIUM_LAUNCHER_READY`.
