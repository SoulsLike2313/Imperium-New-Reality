# TESTS — PATCH-PACK-LIFECYCLE-LAUNCHER-COMMANDS-0001

Validator checks:

- launcher command matrix exists;
- updated `imperium_cli.py` exists;
- preflight/scope/smoke/lifecycle commands run for this Patch Pack;
- `patch run` is rejected;
- operator lifecycle receipt is written.
