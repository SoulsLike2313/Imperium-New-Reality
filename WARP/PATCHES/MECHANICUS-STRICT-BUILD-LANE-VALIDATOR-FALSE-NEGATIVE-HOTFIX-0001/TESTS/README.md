# TESTS — MECHANICUS-STRICT-BUILD-LANE-VALIDATOR-FALSE-NEGATIVE-HOTFIX-0001

Validator checks:

- base strict build validator v0.2 installed;
- base strict build validator passes;
- base receipt is PASS;
- build report is PASS with zero blocking failures;
- planner strict build gap is gone.
