# TESTS — MECHANICUS-LANGUAGE-SURFACE-V2-TOOLCHAIN-PROBE-NONBLOCKING-HOTFIX-0001

Validator checks:

- nonblocking toolchain probe is installed;
- probe runs and writes toolchain report;
- report records not-claimed/debt boundary;
- previous validator reruns and passes;
- previous receipt becomes PASS.
