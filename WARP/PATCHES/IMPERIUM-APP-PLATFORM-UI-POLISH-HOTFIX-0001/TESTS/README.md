# TESTS — IMPERIUM-APP-PLATFORM-UI-POLISH-HOTFIX-0001

Validator checks:

- `main.js` exists;
- `main.js` imports `./styles.css`;
- app platform room markers still exist;
- `styles.css` exists;
- style markers for app shell, hero, rooms, organ grid, aquarium and scrollbars exist;
- UI polish explicitly says truth remains in receipts.
