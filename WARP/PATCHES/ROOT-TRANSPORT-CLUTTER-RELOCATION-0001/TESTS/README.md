# TESTS — ROOT-TRANSPORT-CLUTTER-RELOCATION-0001

Valid if:

- root has no `APPLY_*.ps1`;
- root has no `*_FILE_MANIFEST_SHA256.json`;
- files are moved to `SUPPORT/TRANSPORT`;
- SHA256 before/after matches;
- Administratum registry exists;
- Mechanicus receipt/report exists.
