# IMPERIUM_PHASE7_CLAIM_RECONCILIATION_0001

Phase 7 is fully machine-driven. No UI button clicks are required.

RUN:
- applies Phase 7 implementation;
- updates both Known Gaps authorities;
- makes the Thin IDE fail closed for historical organ rows;
- runs targeted tests, full Python regression, Rust tests/check and frontend build;
- runs provisional reconciliation and an independent disk audit in the patch runtime;
- does not create final repository receipts.

After RUN passes, commit the implementation.

FINALIZE:
- requires a committed clean implementation HEAD descended from `dc3079e4ac9e299761317d6bc078ce1ac3f18c8d`;
- reruns all checks;
- writes current claim authority, claim/evidence matrix, issue registry,
  reconciled Owner result, Phase 7 receipt and independent disk audit;
- expected result is `REFERENCE_CORRIDOR_PASS_WITH_DEBT`;
- does not land or mutate Reality/master.

Current accepted debt:
- the live UI action can temporarily make the window appear unresponsive;
- historical Great Nine/Throne rows are not promoted;
- no land is authorized.
