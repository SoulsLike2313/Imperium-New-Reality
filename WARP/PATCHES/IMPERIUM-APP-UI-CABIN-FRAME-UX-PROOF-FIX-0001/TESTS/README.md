# TESTS — IMPERIUM-APP-UI-CABIN-FRAME-UX-PROOF-FIX-0001

Validator checks:

- `main.js` exists;
- exact `No fake execution claimed` marker exists;
- all previous required main.js markers exist after fix;
- previous cabin-frame validator reruns and passes;
- previous cabin-frame receipt becomes PASS.
