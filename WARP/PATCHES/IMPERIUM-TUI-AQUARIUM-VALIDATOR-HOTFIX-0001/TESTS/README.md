# TESTS — IMPERIUM-TUI-AQUARIUM-VALIDATOR-HOTFIX-0001

Expected:

- TUI uses structural forbidden-git rejection;
- validator checks actual subprocess patterns only;
- `--action status` emits `AQUARIUM_LOG:`;
- `--action throne-readout` emits anti-self-deception fields and `AQUARIUM_LOG:`;
- receipt returns `PASS_IMPERIUM_TUI_ASTRONOMICON_CONSOLE_READY`.
