# TESTS — IMPERIUM-APP-TAURI-PROOF-RUN-0001

Expected:

- proof matrix parses;
- foundation receipt is PASS;
- generated artifacts are ignored;
- Vite dev port matches Tauri devUrl;
- npm install passes;
- npm run check:fps passes;
- npm run check:parity passes;
- npm run build passes;
- cargo check passes.
