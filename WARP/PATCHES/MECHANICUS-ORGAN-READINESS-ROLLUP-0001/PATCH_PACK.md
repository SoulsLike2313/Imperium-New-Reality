# PATCH PACK — MECHANICUS-ORGAN-READINESS-ROLLUP-0001

status: `WARP_CANDIDATE`  
owner: `MECHANICUS + CUSTODES + THRONE`  
mode: `ORGAN_READINESS_ROLLUP_FOUNDATION`

## Purpose

Create the first script-generated current truth rollup for Mechanicus as an organ.

This patch does **not** assemble Mechanicus. It creates a measured, validator-backed readout that lists:

- proven baseline / present candidate Mechanicus capabilities;
- partial or host-bound capabilities;
- assembly gate map from `ORGAN_ASSEMBLY_TARGET_V0_1`;
- forbidden claims;
- current blockers;
- next patch queue;
- deferred future capabilities;
- local model membrane hook marked as `DEFERRED_AFTER_CORE_V1`.

## Owner doctrine captured

Imperium Core v1 remains code/script-first. The local model membrane is acknowledged as a future layer only. It must not be treated as a near-term dependency, a validator authority, a shell executor, or a source of PASS.

## What lands

- Mechanicus rollup law;
- Mechanicus rollup scoring matrix;
- Custodes prosecutor target matrix;
- Throne crown gate target matrix;
- rollup builder tool;
- rollup validator;
- runtime reports/receipts generated by validator.

## Expected validator verdict

`PASS_MECHANICUS_ORGAN_READINESS_ROLLUP_READY`

This means the rollup exists and preserves truth. It does **not** mean:

- `MECHANICUS_ASSEMBLED`;
- `CORE_V1_READY`;
- `CUSTODES_TRUST_PROVEN`;
- `THRONE_CONFIRMED`;
- `LOCAL_MODEL_READY`.

## Boundary

This is a visibility and anti-fake-green patch. It prepares the next work order but does not close the gates.


## Owner-host run protocol

This patch is packaged for the standard Imperium WARP extraction ritual: the zip expands into `WARP/PATCHES/MECHANICUS-ORGAN-READINESS-ROLLUP-0001`, then the Owner runs the patch runner from repo root.

```powershell
cd E:\IMPERIUM_REALITY

$Zip = Get-ChildItem -Path @(
  ".",
  "$env:USERPROFILE\Downloads",
  "$env:USERPROFILE\Desktop"
) -Filter "mechanicus_organ_readiness_rollup_0001*.zip" -File -ErrorAction SilentlyContinue |
  Sort-Object LastWriteTime -Descending |
  Select-Object -First 1

if (-not $Zip) {
  throw "Zip не найден. Скачай mechanicus_organ_readiness_rollup_0001.zip и положи его в E:\IMPERIUM_REALITY или Downloads."
}

Expand-Archive -LiteralPath $Zip.FullName -DestinationPath . -Force

pwsh WARP/PATCHES/MECHANICUS-ORGAN-READINESS-ROLLUP-0001/RUN_MECHANICUS_ORGAN_READINESS_ROLLUP.ps1
```

The runner enforces `pwsh 7.6.2`, copies `FILES_TO_LAND` into the repo, runs the Python validator with `--apply`, and prints `MECHANICUS ORGAN READINESS ROLLUP PASS` only after the validator succeeds.

## Next intended patch after this

Recommended next: `MECHANICUS-TOOL-ADMISSION-V2-0001` or `MECHANICUS-DEPENDENCY-INVENTORY-0001`, depending on Owner priority.
