# TESTS — IMPERIUM-APP-UI-RIGHT-RAIL-COMMAND-DECK-V3-0001

Validator checks:

- `main.js` contains right rail markers and preserves UX actions.
- `styles.css` contains right rail command deck v3 markers.
- viewport-fit internal zones remain.
- contract exists and says final target UI is not reached.
