# Response Contract Engine

Officio stores response shapes per role and mode.

V0.1 engine behavior:

- load role contract files from `RESPONSE_CONTRACTS/`
- embed the selected contract into `settings-get` and role packs
- enforce compact Owner-facing format for Servitor

