# Requirement Matrix Discipline

Officio maintains machine-readable requirement rows with explicit statuses.

Use:

- `requirement_template.json` for row shape
- `requirements-compile` command to generate matrix artifacts from task packs

