# Runtime Output Policy

Default runtime destination:
- `IMPERIUM_NEW_GENERATION/RUNS/ADMINISTRATUM_AGENT/<run_id>/`

Rules:
- do not write runtime outputs into tracked architecture locations by default;
- keep reports/receipts in RUNS for execution evidence;
- optional promoted summaries may be copied into agent REPORTS/RECEIPTS only when intentionally required.
